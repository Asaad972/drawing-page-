# Doodle Deadline

A tiny drawing pad with a built-in countdown, used for the drawing rounds in a Discord "Draw & Write" party game.

Static single-page app. Pass `?t=SECONDS` to set the timer (default 150).
